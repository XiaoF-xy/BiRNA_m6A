# Common packages for the BiRNA-BERT NUC baseline.
# Install torch first with the CUDA command that matches your server, then:
#   pip install -r requirements_birna.txt
transformers==4.41.2
tokenizers==0.19.1
einops==0.8.0
numpy==1.26.4
scikit-learn==1.5.2
tqdm==4.66.5
peft==0.11.1
